## React Router & GitHub Profile Tracker

A multi-page React application built to master client-side routing using `react-router-dom`. It features seamless navigation across Home, About, and Contact pages, alongside a dynamic profile section that fetches and displays live user data and follower counts directly from the GitHub API using advanced route loaders.
