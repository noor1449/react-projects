A highly functional tool that generates secure, randomized passwords based on user-selected criteria (length slider, numbers checkbox, special characters checkbox).
* **Key Concepts:** Deep dive into `useEffect` for optimization, `useCallback` for memoization, and `useRef` to handle the "Copy to Clipboard" feature smoothly.
