## React Todo App with Context API & LocalStorage

A functional task management application built using the Context API to efficiently handle global state and CRUD operations across components. It integrates browser `localStorage` to automatically persist, update, and retrieve the user's todo items, ensuring data isn't lost when the page refreshes.
