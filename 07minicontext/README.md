## React Context API Mini-Profile App

A lightweight React application designed to master global state management using the Context API, completely eliminating the need for prop drilling. It captures a user's login credentials via a simple form and securely passes that state across components to instantly render a personalized welcome message upon submission.
