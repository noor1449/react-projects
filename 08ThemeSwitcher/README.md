A clean dashboard/card component that seamlessly toggles the entire website between Light Mode and Dark Mode with a single switch button.
* **Key Concepts:** Introduction to the **Context API** (`useContext`), managing global states without prop-drilling, and shifting themes by directly manipulating the HTML/Tailwind configuration class.

